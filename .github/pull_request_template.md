## What

One-paragraph summary of what this PR changes and why.

## How

- List the key code changes.
- Link any design notes in `docs/`.

## Tests

- [ ] Added new unit tests covering the change.
- [ ] Updated existing tests if behaviour changed.
- [ ] Ran `pytest` locally — all 35+ tests pass.
- [ ] Ran `python -m contextops_bench smoke` — no exceptions.

## Acceptance criteria

- [ ] No new `MUST` criteria in `docs/ACCEPTANCE.md` are violated.
- [ ] If a new criterion is introduced, it's added to `ACCEPTANCE.md`.

## Checklist

- [ ] `CHANGELOG.md` updated under "Unreleased" (if user-visible).
- [ ] Public API symbols exported via `contextops/__init__.py`.
- [ ] Docstrings added/updated.
- [ ] No unrelated refactors mixed in (split into separate PR if needed).